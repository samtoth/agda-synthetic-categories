\date{2025-02-03}


\subtree{
  \taxon{example}
  \p{ We already know that if #{p : \mathbb{Z}} is a prime, then #{\mathbb{Z}/(p)} is a field.
     This means that #{(p)} is a prime ideal for each #{p} prime.
  }
}

\subtree{
  \taxon{example}
  \p{
    For any #{\lambda : \mathbb{C}}, the quotient #{\mathbb{C}[X]/(X - \lambda) \cong \mathbb{C}}. The proof goes through by the isomorphism theorem, and a map #{\phi} which is evaluation at #{\lambda}.
  }
}