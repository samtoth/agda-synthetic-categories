\date{2025-03-04}
\taxon{Theorem}
\title{Fibres of maps between Sigma types}
\import{stt-macros}

% ```agda
\agda{
module Foundations.TotalEquiv where

open import Foundations.Universes
open import Foundations.QuasiIsomorphism
open import Foundations.CoherentIsomorphism
open import Foundations.Sigma
open import Foundations.SigmaPath
open import Foundations.SigmaProperties
open import Foundations.FibrewiseEquiv
open import Foundations.Identity
open import Foundations.DependentIdentity
open import Foundations.Functions
open import Foundations.EquivContrFibre
open import Foundations.Singleton
open import Foundations.EquivSingleton
}
% ```

\subtree[fre-0033]{
\taxon{Definition}
\title{Functorial action of Σ-types}

% ```agda
\agda{
map-Σ
  : {𝓤1 𝓤2 𝓤3 𝓤4 : Level}
    {A : Type 𝓤1}
    {B : Type 𝓤2}
    {X : A → Type 𝓤3}
    {Y : B → Type 𝓤4}
    (f : A → B)
    (g : (a : A) → X a → Y (f a))
  → Σ A X → Σ B Y
map-Σ f g = total-map-fst f ∘ total-map g
}
% ```
}

\subtree[stt-0027]{
\taxon{Theorem}
\title{Classifying the fibre of total maps}

% ```agda
\agda{
module _
  {𝓤1 𝓤2 𝓤3 𝓤4}
  {A : Type 𝓤1}
  {B : Type 𝓤2}
  (f : A → B)
  {X : A → Type 𝓤3}
  {Y : B → Type 𝓤4}
  (g : (a : A) → X a → Y (f a))
  where

  fibre-map-Σ'
    : (b : B)
      (y : Y b)
    → fibre (map-Σ f g) (b , y)
    ≃ Σ A (λ a → Σ (f a ＝ b) (λ p → Σ (X a) (λ x → IdP (ap Y p) (g a x) y)))
  fibre-map-Σ' b y
    = Σ (Σ A X) (λ (a , x) → (f a , g a x) ＝ (b , y))                    ≃⟨ Σ-assoc ⟩
      Σ A (λ a → Σ (X a) λ x → (f a , g a x) ＝ (b , y))                  ≃⟨ Σ-ap-≃ (λ a → Σ-ap-≃ (λ x → Σ-path e⁻¹)) ⟩
      Σ A (λ a → Σ (X a) λ x → Σ (f a ＝ b)
                                 (λ p → IdP (ap Y p) (g a x) y))          ≃⟨ Σ-ap-≃ (λ a → Σ-comm) ⟩
      Σ A (λ a → Σ (f a ＝ b) λ p → Σ (X a) λ x → IdP (ap Y p) (g a x) y) ≃∎

  fibre-map-Σ
    : (b : B)
      (y : Y b)
    → fibre (map-Σ f g) (b , y)
    ≃ Σ[ af ∶ fibre f b ]
        fibre (λ x → coe (ap Y (af .snd)) (g (af .fst) x)) y
  fibre-map-Σ b y
    = fibre (map-Σ f g) (b , y)                        ≃⟨ fibre-map-Σ' b y ⟩
      Σ A (λ a → Σ (f a ＝ b) λ p → Σ (X a)
        λ x → IdP (ap Y p) (g a x) y)                  ≃⟨ Σ-assoc e⁻¹ ⟩
      Σ (fibre f b) (λ af → Σ (X (af .fst))
        λ x → IdP (ap Y (af .snd)) (g (af .fst) x) y)  ≃∎

fibre-total-map
  : {𝓤 𝓥 𝓦 : Level}
    {A : Type 𝓤} {B : A → Type 𝓥} {B' : A → Type 𝓦}
    {f : ∀ a → B a → B' a}
    {a : A} {b : B' a}
  → fibre (total-map f) (a , b) ≃ fibre (f a) b
fibre-total-map {A = A} {B} {B'} {f = f} {a} {b}
  = fibre (total-map f) (a , b)                         ≃⟨ fibre-map-Σ' id f a b ⟩
    Σ A (λ a' → Σ (a' ＝ a) λ p → Σ (B a')
        λ ba → IdP (ap B' p) (f a' ba) b)               ≃⟨ Σ-＝singl ⟩
    fibre (f a) b                                       ≃∎

fibre-total-map-fst
  : {𝓤 𝓥 𝓦 : Level}
    {A : Type 𝓤}
    {A' : Type 𝓥}
    {B : A → Type 𝓦}
    (f : A' → A)
    (a : A)
    (b : B a)
  → fibre (total-map-fst f {B}) (a , b) ≃ fibre f a
fibre-total-map-fst {A = A} {A'} {B} f a b
  = fibre (total-map-fst f {B}) (a , b)                           ≃⟨ fibre-map-Σ f (λ _ → id) a b ⟩
    Σ (fibre f a) (λ af → fibre (λ x → coe (ap B (af .snd)) x) b) ≃⟨ Σ-singleton (λ af → is-contr-map←is-equiv (coe-is-equiv (ap B (af .snd))) b) ⟩
    fibre f a                                                     ≃∎

module _
  {𝓤1 𝓤2 𝓤3 𝓤4}
  {A : Type 𝓤1}
  {B : Type 𝓤2}
  (f : A → B)
  {Y : B → Type 𝓤4}
  where

  fibre-map-Σ-over-at
    : {X : B → Type 𝓤3}
      (g : (b : B) → X b → Y b)
      (b : B)
      (y : Y b)
      (af : fibre f b)
    → fibre (λ x → coe (ap Y (af .snd)) (g (f (af .fst)) x)) y
    ≃ fibre (g b) y
  fibre-map-Σ-over-at g b y (_ , refl) = idequiv

  fibre-map-Σ-over
    : {X : B → Type 𝓤3}
      (g : (b : B) → X b → Y b)
      (b : B) (y : Y b)
    → fibre (map-Σ f (λ a → g (f a))) (b , y)
    ≃ (fibre f b × fibre (g b) y)
  fibre-map-Σ-over g b y
    = fibre (map-Σ f (λ a → g (f a))) (b , y)    ≃⟨ fibre-map-Σ f (λ a → g (f a)) b y ⟩
      Σ[ (a , p) ∶ fibre f b ]
        fibre (λ x → coe (ap Y p) (g (f a) x)) y ≃⟨ Σ-ap-≃ (fibre-map-Σ-over-at g b y) ⟩
      (fibre f b × fibre (g b) y)                ≃∎

  fibre-map-Σ-over-left
    : {X : B → Type 𝓤3}
      (g : (b : B) → X b → Y b)
      (b : B) (y : Y b)
      (a : A) (x : X (f a))
      (p : (f a , g (f a) x) ＝ (b , y))
     → _≃_.bwd fibre-total-map (snd (_≃_.fwd (fibre-map-Σ-over g b y) ((a , x) , p)))
     ＝ ((f a , x) , p)
  fibre-map-Σ-over-left g b y a x refl = refl

}
% ```
}

\subtree[stt-0028]{
\taxon{Corollary}

% ```agda
\agda{
is-fibrewise-equiv←is-total-equiv
  : ∀ {𝓤 𝓥 𝓦} {A : Type 𝓤} {B : A → Type 𝓥}
      {B' : A → Type 𝓦} {f : ∀ a → B a → B' a}
    → is-equiv (total-map f) → is-fibrewise-equiv f
is-fibrewise-equiv←is-total-equiv {f = f} teq a
  = is-equiv←is-contr-map λ
      b → is-single←equiv-to-single
            fibre-total-map
            (is-contr-map←is-equiv teq (a , b))

fibrewise⇔total-is-equiv
  : ∀ {𝓤 𝓥 𝓦} {A : Type 𝓤} {B : A → Type 𝓥}
      {B' : A → Type 𝓦} {f : ∀ a → B a → B' a}
    → is-equiv (total-map f) ⇔ is-fibrewise-equiv f
fibrewise⇔total-is-equiv
  = ( is-fibrewise-equiv←is-total-equiv
    , is-total-equiv←is-fibrewise-equiv)
}
% ```
}
