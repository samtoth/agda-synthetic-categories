\date{2025-02-03}
\taxon{Proposition}

\p{
If #{A} is an integral domain, then so is #{A[X]}.

In addition, for #{f,g \in A[X]}, #{\textrm{deg}(fg) = \textrm{def}(f) + \textrm{deg}(g)}
}

\subtree{
\taxon{corollary}
  \p{ If #{\mathbb{K}} is a field, then so is #{\mathbb{K}[X_1, ..., X_n]}.}
}
